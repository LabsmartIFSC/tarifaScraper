import utils.py
import json

"""	table = html.find('table')
	tableDic = {}

	categoria = table.find('caption').get_text()
	tableDic.update({"categoria":categoria})
	for row in table.find_all('tr'):
		linha = row.find_all('td')
		if len(linha) > 3:
			if subgrupo is not None:
				subgrupo = {"subgrupo":linha[0].get_text()}
				count=0;
			else
				tableDic.update({subgrupo["subgrupo"]:subgrupo})
				subgrupo = {"subgrupo":linha[0].get_text()}
				count=0;
		
		
			tituloClassificacao = "classificacao"+count
			count+=1
			subgrupo.update({tituloClassificacao:

	print(json.dumps(tableDic, indent = 4))
"""
